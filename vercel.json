{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "yarn install && yarn build",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}